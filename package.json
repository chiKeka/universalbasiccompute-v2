{
  "name": "ubcv2",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Basic Compute — agentic platform for provisioning free-tier cloud services",
  "scripts": {
    "ubc": "pnpm --filter @ubcv2/cli exec tsx src/index.ts",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
